#include<iostream>
using namespace std;
#include"deque.h"

int main(){
    // Create queue
    /* Member functions

    * constructor
    * copy constructor
    * operator=
    * assign
    */

   // Modifiers
   /*
    * push_front() : O(1)
    * push_back() : O(1)
    * pop_front() : O(1)
    * pop_back() : O(n) = Because it need to traversal for reach to last node
    * clean() : O(n)
   */
}
