#include"StackUsingLinkedList.h"
// -----------------------------------------------------------------------------

int main() {
    // Create stack
    /* Constructor
     * Copy Constructor
     * operator=
    */

    /* Access */
    /* top() : Return top element. O(1)*/

    /* Modifiers */
    /* push() : Insert element on stack
     * pop() : Delete element from stack
     * clean() : clear stack
    */

    /* size */
    /* getSize() : return how many element is inserted in stack
     * empty() : check stack is empty or not
    */
}
